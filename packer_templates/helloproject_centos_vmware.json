{
  "variables": {
    "version": "1908",
    "helloproject_user": "helloprojectadmin",
    "absolut_path": "/Users/elian/Google Drive/helloproject_deploy",
    "absolut_path_vm": "/Users/elian/Downloads"
  },

  "builders": [
    {
      "vm_name": "helloproject-centos-{{ user `version` }}",
      "iso_urls": [
        "iso/CentOS-7-x86_64-Minimal-{{ user `version` }}.iso",
        "http://ftp.unicamp.br/pub/centos/7.7.{{ user `version` }}/isos/x86_64/CentOS-7-x86_64-Minimal-{{ user `version` }}.iso"
      ],
      "iso_checksum_url": "file:{{ user `absolut_path` }}/iso/CentOS-7-x86_64-Minimal-{{ user `version` }}_sha256sum.txt",
      "iso_checksum_type": "file",
      "iso_target_path": "{{ user `absolut_path` }}/iso/CentOS-7-x86_64-Minimal-{{ user `version` }}.iso",
      "http_directory": "http",
      "output_directory": "{{ user `absolut_path_vm` }}/helloproject-centos-{{ user `version` }}",
      "boot_command": [
        "<esc>",
        "<wait>",        
        "linux inst.ks=http://{{.HTTPIP}}:{{.HTTPPort}}/helloproject_ks.cfg biosdevname=0 net.ifnames=0",
        "<enter>"
      ],
      "boot_wait": "10s",
      "disk_size": 10240,
      "memory": 512,
      "cpus": 1,
      "disk_adapter_type": "sata",
      "cdrom_adapter_type": "sata",
      "tools_upload_flavor": "linux",
      "guest_os_type": "centos7_64Guest",
      "shutdown_command": "echo 'halt -p' > shutdown.sh; echo 'vagrant'|sudo -S sh 'shutdown.sh'",
      "type": "vmware-iso",
      "ssh_username": "{{ user `helloproject_user` }}",
      "ssh_private_key_file": "files/helloproject_key",
      "ssh_port": 22,
      "ssh_timeout": "90m",
      "headless": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "environment_vars": [
        "HELLOPROJECTUSER={{ user `helloproject_user` }}"
      ],
      "scripts": [
        "scripts/builder_tools.sh",
        "scripts/clean.sh"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "output": "boxes/centos-7.7.{{ user `version` }}-x86_64-{{.Provider}}.box"
    }
  ]
}
